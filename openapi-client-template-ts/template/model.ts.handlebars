{{#each components.schemas}}
export class {{@key}} {
  {{#each properties}}
    {{#if description}}
    // {{description}}
    {{/if}}
    public {{@key}}{{#unless _required}}?{{/unless}}: {{typeConvert this}};
  {{/each}}
}
{{/each}}